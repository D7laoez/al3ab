const C=window.STORE_CONFIG||{shipping:{},currency:'د.ع'};
const $=s=>document.querySelector(s),money=n=>new Intl.NumberFormat('ar-IQ').format(n)+' '+(C.currency||'د.ع');
const PRODUCTS=[
{id:'1',name:'فحمات فرامل أمامية أصلية',cat:'قطع غيار',price:45000,old:null,rating:4.9,stock:12,brand:'OEM',icon:'🛞',badge:'الأكثر مبيعاً',fit:['Toyota','Hyundai','Kia']},
{id:'2',name:'فلتر زيت Premium للمحركات',cat:'زيوت',price:12000,old:null,rating:4.8,stock:35,brand:'Premium',icon:'🔩',badge:'عرض',fit:['Toyota','Hyundai','Kia','Nissan']},
{id:'3',name:'زيت محرك 5W-30 تخليقي',cat:'زيوت',price:38000,old:45000,rating:4.9,stock:20,brand:'Ultra',icon:'🛢️',badge:'خصم',fit:['Universal']},
{id:'4',name:'شاحن سيارة سريع USB-C 45W',cat:'كهربائيات',price:18000,old:null,rating:4.7,stock:50,brand:'Volt',icon:'🔌',badge:'جديد',fit:['Universal']},
{id:'5',name:'كمبروسر هواء رقمي',cat:'أدوات',price:32000,old:null,rating:4.8,stock:8,brand:'ProTool',icon:'💨',badge:'مميز',fit:['Universal']},
{id:'6',name:'دعاسات سيارة جلدية فاخرة',cat:'إكسسوارات',price:65000,old:null,rating:4.9,stock:6,brand:'Lux',icon:'🚘',badge:'جديد',fit:['Toyota','Hyundai','Kia']},
{id:'7',name:'جهاز فحص OBD2 ذكي',cat:'أدوات',price:42000,old:null,rating:4.8,stock:14,brand:'ScanPro',icon:'📟',badge:'الأكثر طلباً',fit:['Universal']},
{id:'8',name:'لمبات LED للسيارة H7',cat:'كهربائيات',price:28000,old:34000,rating:4.7,stock:25,brand:'Bright',icon:'💡',badge:'عرض',fit:['Universal']}
];
const GOV=C.shipping||{};
let activeCat='all',sort='featured';
let cart=JSON.parse(localStorage.getItem('al3ab-cart')||'[]');
let favs=JSON.parse(localStorage.getItem('al3ab-favorites')||'[]');
let orders=JSON.parse(localStorage.getItem('al3ab-orders')||'[]');
const get=id=>PRODUCTS.find(p=>String(p.id)===String(id));
function persist(){localStorage.setItem('al3ab-cart',JSON.stringify(cart));localStorage.setItem('al3ab-favorites',JSON.stringify(favs));localStorage.setItem('al3ab-orders',JSON.stringify(orders));}
function subtotal(){return cart.reduce((t,x)=>t+(get(x.id)?.price||0)*x.qty,0)}
function renderProducts(){
 let q=($('#searchInput').value||'').trim().toLowerCase(),list=PRODUCTS.filter(p=>(activeCat==='all'||p.cat===activeCat)&&(!q||`${p.name} ${p.cat} ${p.brand}`.toLowerCase().includes(q)));
 if(sort==='low')list=[...list].sort((a,b)=>a.price-b.price);if(sort==='high')list=[...list].sort((a,b)=>b.price-a.price);if(sort==='rating')list=[...list].sort((a,b)=>b.rating-a.rating);
 $('#productGrid').innerHTML=list.length?list.map(p=>`<article class="product"><div class="product-img"><span class="badge">${p.badge}</span><button class="fav ${favs.includes(p.id)?'active':''}" data-fav="${p.id}">${favs.includes(p.id)?'♥':'♡'}</button><span>${p.icon}</span></div><div class="product-body"><div class="product-cat">${p.cat} · ${p.brand}</div><h3>${p.name}</h3><div class="stars">★★★★★ <span>(${p.rating})</span></div><div class="stock ${p.stock<10?'low':''}">${p.stock<10?`متبقي ${p.stock} فقط`:'متوفر بالمخزن'}</div><div class="price">${money(p.price)} ${p.old?`<span class="old">${money(p.old)}</span>`:''}</div><button class="add" data-add="${p.id}">أضف للسلة +</button></div></article>`).join(''):'<div class="empty" style="grid-column:1/-1">🔎<b>ما لقينا منتجات مطابقة</b><span>جرّب بحثاً آخر.</span></div>';
}
function renderCart(){
 $('#cartCount').textContent=cart.reduce((n,x)=>n+x.qty,0);$('#cartTotal').textContent=money(subtotal());
 $('#cartItems').innerHTML=cart.length?cart.map(x=>{let p=get(x.id);return p?`<div class="cart-row"><div class="cart-thumb">${p.icon}</div><div><h4>${p.name}</h4><small>${money(p.price)}</small><div class="qty"><button data-minus="${p.id}">−</button><b>${x.qty}</b><button data-plus="${p.id}">+</button></div></div><strong>${money(p.price*x.qty)}</strong></div>`:''}).join(''):'<div class="empty">🛒<b>السلة فارغة</b><span>أضف المنتجات وتابع الطلب.</span></div>';
}
function openCart(){$('#cartDrawer').classList.add('open');$('#overlay').classList.add('open')}function closeCart(){$('#cartDrawer').classList.remove('open');$('#overlay').classList.remove('open')}
function add(id){let p=get(id);if(!p||p.stock<1)return;let x=cart.find(i=>i.id===p.id);x?x.qty=Math.min(x.qty+1,p.stock):cart.push({id:p.id,qty:1});persist();renderCart();openCart()}
function qty(id,d){let x=cart.find(i=>i.id===String(id));if(!x)return;x.qty+=d;if(x.qty<1)cart=cart.filter(i=>i.id!==String(id));let p=get(id);if(p)x.qty=Math.min(x.qty,p.stock);persist();renderCart()}
function modal(h){$('#modalContent').innerHTML=h;$('#modal').classList.add('open')}function closeModal(){$('#modal').classList.remove('open')}
function filterCategory(c){activeCat=c;renderProducts();document.querySelector('#shop').scrollIntoView({behavior:'smooth'});$('#categories').classList.remove('open')}
function favorites(){let ps=favs.map(get).filter(Boolean);modal(`<button class="close-modal" onclick="closeModal()">×</button><h2>المفضلة ❤️</h2>${ps.length?`<div class="mini-list">${ps.map(p=>`<button onclick="add('${p.id}');closeModal()">${p.icon} ${p.name}<b>${money(p.price)}</b></button>`).join('')}</div>`:'<div class="empty">♡<b>المفضلة فارغة</b><span>اضغط القلب على المنتج.</span></div>'}`)}
function showOrders(){modal(`<button class="close-modal" onclick="closeModal()">×</button><h2>طلباتي 📦</h2>${orders.length?`<div class="orders-list">${orders.map(o=>`<div class="order-card"><b>${o.no}</b><span>${o.status}</span><strong>${money(o.total)}</strong><small>${new Date(o.date).toLocaleString('ar-IQ')}</small></div>`).join('')}</div>`:'<div class="empty">لا توجد طلبات بعد.</div>'}`)}
function fitment(){modal(`<button class="close-modal" onclick="closeModal()">×</button><h2>🔎 ساعدني أختار القطعة</h2><form class="checkout-form" id="fitForm"><div class="form-row"><select id="fitMake" required><option value="">الماركة</option><option>Toyota</option><option>Hyundai</option><option>Kia</option><option>Nissan</option></select><input required placeholder="الموديل"></div><div class="form-row"><input type="number" min="1980" max="2035" placeholder="السنة"><select id="fitCat" required><option value="">نوع القطعة</option>${['قطع غيار','زيوت','إكسسوارات','كهربائيات','أدوات'].map(x=>`<option>${x}</option>`).join('')}</select></div><button class="primary full">عرض القطع المناسبة</button></form>`);$('#fitForm').onsubmit=e=>{e.preventDefault();filterCategory($('#fitCat').value);closeModal()}}
function checkout(){
 if(!cart.length)return modal('<button class="close-modal" onclick="closeModal()">×</button><h2>السلة فارغة</h2>');let sub=subtotal();
 modal(`<button class="close-modal" onclick="closeModal()">×</button><h2>إتمام الطلب</h2><div class="order-summary"><span>المنتجات</span><b>${money(sub)}</b></div><form class="checkout-form" id="orderForm"><div class="form-row"><input id="nm" required placeholder="الاسم الكامل"><input id="ph" required inputmode="tel" placeholder="رقم الهاتف 07XXXXXXXXX"></div><div class="form-row"><select id="gov" required><option value="">المحافظة</option>${Object.keys(GOV).map(g=>`<option>${g}</option>`).join('')}</select><input id="area" required placeholder="المنطقة / القضاء"></div><textarea id="addr" rows="3" required placeholder="العنوان بالتفصيل وأقرب نقطة دالة"></textarea><select id="pay"><option>الدفع عند الاستلام</option><option>تحويل إلكتروني</option></select><div class="shipping-line"><span>الشحن</span><b id="sf">اختر المحافظة</b></div><div class="shipping-line total"><span>الإجمالي</span><b id="ft">${money(sub)}</b></div><button class="primary full">تأكيد الطلب</button></form>`);
 $('#gov').onchange=()=>{let fee=Number(GOV[$('#gov').value]||0);$('#sf').textContent=money(fee);$('#ft').textContent=money(sub+fee)};
 $('#orderForm').onsubmit=e=>{e.preventDefault();let ph=$('#ph').value.replace(/\s/g,'');if(!/^07\d{9}$/.test(ph))return alert('رقم الهاتف يجب أن يبدأ بـ 07 ويتكون من 11 رقم');let shipping=Number(GOV[$('#gov').value]||0),no='CAR-'+Date.now().toString().slice(-8),o={no,date:new Date().toISOString(),name:$('#nm').value,phone:ph,governorate:$('#gov').value,area:$('#area').value,address:$('#addr').value,payment:$('#pay').value,total:sub+shipping,status:'قيد المراجعة',items:[...cart]};orders.unshift(o);cart=[];persist();renderCart();closeModal();modal(`<button class="close-modal" onclick="closeModal()">×</button><div class="success-box"><div>✅</div><h2>تم استلام طلبك</h2><p>رقم الطلب <b>${no}</b></p><p>الإجمالي <b>${money(o.total)}</b></p><button class="primary" onclick="closeModal();showOrders()">عرض الطلب</button></div>`)}}
function track(){modal(`<button class="close-modal" onclick="closeModal()">×</button><h2>📦 تتبع الطلب</h2><form class="checkout-form" id="tr"><input id="trn" required placeholder="CAR-12345678"><button class="primary full">بحث</button></form><div id="trr"></div>`);$('#tr').onsubmit=e=>{e.preventDefault();let o=orders.find(x=>x.no===$('#trn').value.trim());$('#trr').innerHTML=o?`<div class="order-card"><b>${o.no}</b><span>${o.status}</span><strong>${money(o.total)}</strong></div>`:'<div class="empty">لم نجد هذا الطلب.</div>'}}
function account(){modal(`<button class="close-modal" onclick="closeModal()">×</button><h2>حسابي 👤</h2><p class="muted">الحساب الحقيقي جاهز للربط مع Supabase عند وضع مفاتيح المشروع في config.js.</p><button class="primary full" onclick="showOrders()">طلباتي (${orders.length})</button>`)}
function info(t,b){modal(`<button class="close-modal" onclick="closeModal()">×</button><h2>${t}</h2><p class="info-text">${b}</p>`)}
$('#searchInput').oninput=renderProducts;$('#searchBtn').onclick=()=>{renderProducts();document.querySelector('#shop').scrollIntoView({behavior:'smooth'})};$('#sortSelect').onchange=e=>{sort=e.target.value;renderProducts()};$('#allProducts').onclick=()=>filterCategory('all');$('#offersBtn').onclick=()=>filterCategory('زيوت');$('#cartBtn').onclick=openCart;$('#closeCart').onclick=closeCart;$('#overlay').onclick=closeCart;$('#checkoutBtn').onclick=checkout;$('#accountBtn').onclick=account;$('#footerAccount').onclick=e=>{e.preventDefault();account()};$('#ordersLink').onclick=e=>{e.preventDefault();showOrders()};$('#favoritesLink').onclick=e=>{e.preventDefault();favorites()};$('#trackOrderLink').onclick=e=>{e.preventDefault();track()};$('#returnsLink').onclick=e=>{e.preventDefault();info('الاستبدال والاسترجاع','يتم فحص المنتج عند الاستلام. للاستبدال تواصل مع خدمة العملاء مع رقم الطلب وحالة المنتج.')};$('#contactLink').onclick=e=>{e.preventDefault();info('تواصل معنا','خدمة العملاء: 0770 000 0000 — سيتم إضافة واتساب والبريد عند تجهيز بيانات المتجر.')};$('#fitmentBtn').onclick=fitment;$('#mobileMenu').onclick=()=>$('#categories').classList.toggle('open');$('#modal').onclick=e=>{if(e.target.id==='modal')closeModal()};
document.addEventListener('click',e=>{let a=e.target.closest('[data-cat]');if(a){e.preventDefault();filterCategory(a.dataset.cat)}let x=e.target.closest('[data-add]');if(x)add(x.dataset.add);let f=e.target.closest('[data-fav]');if(f){let id=f.dataset.fav;favs=favs.includes(id)?favs.filter(x=>x!==id):[...favs,id];persist();renderProducts()}let m=e.target.closest('[data-minus]');if(m)qty(m.dataset.minus,-1);let p=e.target.closest('[data-plus]');if(p)qty(p.dataset.plus,1);let card=e.target.closest('[data-product]');if(card&&!e.target.closest('button'))info('تفاصيل المنتج',`<div class="detail-art">${get(card.dataset.product)?.icon||'🚗'}</div><h3>${get(card.dataset.product)?.name||''}</h3><p>${get(card.dataset.product)?.brand||''} · ${get(card.dataset.product)?.cat||''}</p><b>${money(get(card.dataset.product)?.price||0)}</b>`)})
renderProducts();renderCart();
