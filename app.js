const C=window.STORE_CONFIG||{shipping:{},currency:'د.ع'};
const $=s=>document.querySelector(s);
const num=n=>new Intl.NumberFormat('en-US').format(Number(n)||0);
const money=n=>`${num(n)} ${C.currency||'د.ع'}`;
const PRODUCTS=[
{id:'1',name:'فحمات فرامل أمامية أصلية',cat:'قطع غيار',price:45000,old:50000,rating:4.9,stock:12,brand:'OEM',badge:'الأكثر مبيعاً',image:'https://cdn.awsli.com.br/2500x2500/2695/2695989/produto/263323770250568791e.jpg',fit:['Toyota','Hyundai','Kia']},
{id:'2',name:'فلتر زيت Premium للمحركات',cat:'زيوت',price:12000,old:15000,rating:4.8,stock:35,brand:'Premium',badge:'عرض',image:'https://cdn.autodoc.de/thumb?id=11156535&lng=en&m=0&n=0&rev=94078092',fit:['Toyota','Hyundai','Kia','Nissan']},
{id:'3',name:'زيت محرك 5W-30 تخليقي',cat:'زيوت',price:38000,old:45000,rating:4.9,stock:20,brand:'Ultra',badge:'خصم',image:'https://cdn.autodoc.de/thumb?id=27306321&lng=pt&m=0&n=0&rev=94078027',fit:['Toyota','Hyundai','Kia','Nissan']},
{id:'4',name:'شاحن سيارة سريع USB-C 45W',cat:'كهربائيات',price:18000,old:22000,rating:4.7,stock:50,brand:'Volt',badge:'جديد',image:'https://us.ugreen.com/cdn/shop/products/ugreen-130w-usb-c-car-charger-219754.png?v=1764234772&width=1600',fit:['Universal']},
{id:'5',name:'كمبروسر هواء رقمي',cat:'أدوات',price:32000,old:36000,rating:4.8,stock:8,brand:'ProTool',badge:'مميز',image:'https://ueeshop.ly200-cdn.com/u_file/UPBB/UPBB682/2412/11/products/CAP032-1.jpg',fit:['Universal']},
{id:'6',name:'دعاسات سيارة جلدية فاخرة',cat:'إكسسوارات',price:65000,old:75000,rating:4.9,stock:6,brand:'Lux',badge:'جديد',image:'https://m.media-amazon.com/images/I/71tAwSrkwfL.jpg',fit:['Toyota','Hyundai','Kia']},
{id:'7',name:'جهاز فحص OBD2 ذكي',cat:'أدوات',price:42000,old:49000,rating:4.8,stock:14,brand:'ScanPro',badge:'الأكثر طلباً',image:'https://m.media-amazon.com/images/I/41XfoPw9V6L.jpg',fit:['Universal']},
{id:'8',name:'لمبات LED للسيارة H7',cat:'كهربائيات',price:28000,old:34000,rating:4.7,stock:25,brand:'Bright',badge:'عرض',image:'https://mircaraudio.com/media/store/image0_111267.png',fit:['Universal']}
];
const GOV=C.shipping||{};
let activeCat='all',sort='featured';
let cart=JSON.parse(localStorage.getItem('al3ab-cart')||'[]');
let favs=JSON.parse(localStorage.getItem('al3ab-favorites')||'[]');
let orders=JSON.parse(localStorage.getItem('al3ab-orders')||'[]');
const get=id=>PRODUCTS.find(p=>String(p.id)===String(id));
const esc=s=>String(s).replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[m]));
const img=(p,cls='')=>`<img class="${cls}" src="${p.image}" alt="${esc(p.name)}" loading="lazy" onerror="this.style.display='none';this.nextElementSibling.hidden=false"><span class="image-fallback" hidden>الصورة غير متاحة</span>`;
function persist(){localStorage.setItem('al3ab-cart',JSON.stringify(cart));localStorage.setItem('al3ab-favorites',JSON.stringify(favs));localStorage.setItem('al3ab-orders',JSON.stringify(orders));}
function subtotal(){return cart.reduce((t,x)=>t+(get(x.id)?.price||0)*x.qty,0)}
function renderProducts(){const input=$('#searchInput'),grid=$('#productGrid');if(!input||!grid)return;const q=input.value.trim().toLowerCase();let list=PRODUCTS.filter(p=>(activeCat==='all'||p.cat===activeCat)&&(!q||`${p.name} ${p.cat} ${p.brand}`.toLowerCase().includes(q)));if(sort==='low')list=[...list].sort((a,b)=>a.price-b.price);if(sort==='high')list=[...list].sort((a,b)=>b.price-a.price);if(sort==='rating')list=[...list].sort((a,b)=>b.rating-a.rating);grid.innerHTML=list.length?list.map(p=>`<article class="product" data-product="${p.id}"><div class="product-img">${img(p)}<span class="badge">${p.badge}</span><button class="fav ${favs.includes(p.id)?'active':''}" data-fav="${p.id}" aria-label="المفضلة">${favs.includes(p.id)?'♥':'♡'}</button></div><div class="product-body"><div class="product-cat">${p.cat} · ${p.brand}</div><h3>${p.name}</h3><div class="meta-row"><div class="stars">★★★★★ <span>(${p.rating})</span></div><div class="stock ${p.stock<10?'low':''}">${p.stock<10?`متبقي ${num(p.stock)}`:'متوفر'}</div></div><div class="price-row"><span class="price">${money(p.price)}</span>${p.old?`<span class="old">${money(p.old)}</span>`:''}</div><button class="add" data-add="${p.id}">أضف للسلة</button></div></article>`).join(''):'<div class="empty" style="grid-column:1/-1"><b>ما لقينا منتجات مطابقة</b><span>جرّب اسم القطعة أو الفئة أو الماركة.</span></div>';}
function renderCart(){const count=$('#cartCount'),total=$('#cartTotal'),items=$('#cartItems');if(!count||!total||!items)return;count.textContent=num(cart.reduce((n,x)=>n+x.qty,0));total.textContent=money(subtotal());items.innerHTML=cart.length?cart.map(x=>{const p=get(x.id);return p?`<div class="cart-row"><div class="cart-thumb">${img(p,'cart-image')}</div><div><h4>${p.name}</h4><small>${money(p.price)}</small><div class="qty"><button data-minus="${p.id}">−</button><b class="latin">${num(x.qty)}</b><button data-plus="${p.id}">+</button></div></div><strong>${money(p.price*x.qty)}</strong></div>`:''}).join(''):'<div class="empty"><b>السلة فارغة</b><span>أضف المنتجات وتابع الطلب.</span></div>';}
function openCart(){$('#cartDrawer')?.classList.add('open');$('#overlay')?.classList.add('open')}
function closeCart(){$('#cartDrawer')?.classList.remove('open');$('#overlay')?.classList.remove('open')}
function add(id){const p=get(id);if(!p||p.stock<1)return;const x=cart.find(i=>i.id===p.id);if(x)x.qty=Math.min(x.qty+1,p.stock);else cart.push({id:p.id,qty:1});persist();renderCart();openCart()}
function qty(id,d){const x=cart.find(i=>i.id===String(id));if(!x)return;x.qty+=d;const p=get(id);if(p)x.qty=Math.min(x.qty,p.stock);if(x.qty<1)cart=cart.filter(i=>i.id!==String(id));persist();renderCart()}
function modal(html){const box=$('#modalContent'),m=$('#modal');if(!box||!m)return;box.innerHTML=html;m.classList.add('open')}
function closeModal(){$('#modal')?.classList.remove('open')}
function filterCategory(c){activeCat=c;renderProducts();$('#shop')?.scrollIntoView({behavior:'smooth'});$('#categories')?.classList.remove('open')}
function toggleFav(id){id=String(id);favs=favs.includes(id)?favs.filter(x=>x!==id):[...favs,id];persist();renderProducts()}
function favorites(){const ps=favs.map(get).filter(Boolean);modal(`<button class="close-modal" onclick="closeModal()">×</button><h2>المفضلة</h2>${ps.length?`<div class="mini-list">${ps.map(p=>`<button onclick="add('${p.id}');closeModal()">${img(p,'mini-image')}<span>${esc(p.name)}</span><b>${money(p.price)}</b></button>`).join('')}</div>`:'<div class="empty"><b>المفضلة فارغة</b><span>اضغط القلب على أي منتج.</span></div>'}`)}
function showOrders(){modal(`<button class="close-modal" onclick="closeModal()">×</button><h2>طلباتي</h2>${orders.length?orders.map(o=>`<div class="order-card"><b>${esc(o.no)}</b><span>${esc(o.status)}</span><strong>${money(o.total)}</strong><small>${new Date(o.date).toLocaleDateString('en-GB')}</small></div>`).join(''):'<div class="empty">لا توجد طلبات بعد.</div>'}`)}
function productDetails(id){const p=get(id);if(!p)return;modal(`<button class="close-modal" onclick="closeModal()">×</button><div class="detail-photo">${img(p,'detail-image')}</div><span class="eyebrow eyebrow-dark">${p.cat} · ${p.brand}</span><h2>${esc(p.name)}</h2><div class="stars">★★★★★ <span>(${p.rating})</span></div><div class="detail-price">${money(p.price)}</div><p class="muted">منتج ضمن كتالوج المتجر، مع معلومات واضحة عن السعر والمخزون والتوافق.</p><div class="fit-result">المخزون الحالي: <b class="latin">${num(p.stock)}</b> قطعة</div><div class="detail-actions"><button class="btn btn-primary" onclick="add('${p.id}');closeModal()">أضف للسلة</button><button class="btn" onclick="toggleFav('${p.id}');productDetails('${p.id}')">${favs.includes(p.id)?'♥ محفوظة':'♡ المفضلة'}</button></div>`)}
function fitment(){modal(`<button class="close-modal" onclick="closeModal()">×</button><h2>اختيار القطعة حسب السيارة</h2><p class="muted">حدد الماركة ونوع القطعة حتى نضيّق النتائج.</p><form class="checkout-form" id="fitForm"><div class="form-row"><select id="fitMake" required><option value="">الماركة</option><option>Toyota</option><option>Hyundai</option><option>Kia</option><option>Nissan</option></select><input id="fitModel" required placeholder="الموديل"></div><div class="form-row"><input id="fitYear" type="number" min="1980" max="2035" inputmode="numeric" placeholder="Year"><select id="fitCat" required><option value="">نوع القطعة</option>${['قطع غيار','زيوت','إكسسوارات','كهربائيات','أدوات'].map(x=>`<option>${x}</option>`).join('')}</select></div><button class="btn btn-primary full">عرض النتائج</button></form>`);$('#fitForm').onsubmit=e=>{e.preventDefault();filterCategory($('#fitCat').value);closeModal()}}
function checkout(){if(!cart.length)return modal('<button class="close-modal" onclick="closeModal()">×</button><h2>السلة فارغة</h2>');const sub=subtotal();modal(`<button class="close-modal" onclick="closeModal()">×</button><h2>إتمام الطلب</h2><div class="order-summary"><span>المنتجات</span><b>${money(sub)}</b></div><form class="checkout-form" id="orderForm"><div class="form-row"><input id="nm" required placeholder="الاسم الكامل"><input id="ph" required inputmode="tel" placeholder="07XXXXXXXXX"></div><div class="form-row"><select id="gov" required><option value="">المحافظة</option>${Object.keys(GOV).map(g=>`<option>${g}</option>`).join('')}</select><input id="area" required placeholder="المنطقة / القضاء"></div><textarea id="addr" rows="3" required placeholder="العنوان بالتفصيل وأقرب نقطة دالة"></textarea><select id="pay"><option>الدفع عند الاستلام</option><option>تحويل إلكتروني</option></select><div class="shipping-line"><span>الشحن</span><b id="sf">اختر المحافظة</b></div><div class="shipping-line total"><span>الإجمالي</span><b id="ft">${money(sub)}</b></div><button class="btn btn-primary full">تأكيد الطلب</button></form>`);$('#gov').onchange=()=>{const fee=Number(GOV[$('#gov').value]||0);$('#sf').textContent=money(fee);$('#ft').textContent=money(sub+fee)};$('#orderForm').onsubmit=e=>{e.preventDefault();const ph=$('#ph').value.replace(/\s/g,'');if(!/^07\d{9}$/.test(ph))return alert('رقم الهاتف يجب أن يبدأ بـ 07 ويتكون من 11 رقم');const shipping=Number(GOV[$('#gov').value]||0);const no=`CAR-${String(Date.now()).slice(-8)}`;orders.unshift({no,date:new Date().toISOString(),name:$('#nm').value,phone:ph,governorate:$('#gov').value,area:$('#area').value,address:$('#addr').value,payment:$('#pay').value,total:sub+shipping,status:'قيد المراجعة',items:[...cart]});cart=[];persist();renderCart();closeModal();modal(`<button class="close-modal" onclick="closeModal()">×</button><div class="success"><div class="check">✓</div><h2>تم استلام طلبك</h2><p>رقم الطلب <b>${no}</b></p><button class="btn btn-primary" onclick="closeModal();showOrders()">عرض الطلب</button></div>`)} }
function track(){modal(`<button class="close-modal" onclick="closeModal()">×</button><h2>تتبع الطلب</h2><form class="checkout-form" id="trackForm"><input id="trackNo" placeholder="CAR-12345678" required><button class="btn btn-primary full">بحث</button></form><div id="trackResult"></div>`);$('#trackForm').onsubmit=e=>{e.preventDefault();const o=orders.find(x=>x.no===$('#trackNo').value.trim());$('#trackResult').innerHTML=o?`<div class="order-card"><b>${esc(o.no)}</b><span>${esc(o.status)}</span><strong>${money(o.total)}</strong></div>`:'<div class="empty">لم نجد هذا الطلب.</div>'}}
function account(){modal(`<button class="close-modal" onclick="closeModal()">×</button><h2>حسابي</h2><p class="muted">يمكن ربط الحساب الحقيقي مع Supabase عند تفعيل إعدادات المشروع.</p><button class="btn btn-primary full" onclick="closeModal();showOrders()">طلباتي (${num(orders.length)})</button>`) }
function info(title,text){modal(`<button class="close-modal" onclick="closeModal()">×</button><h2>${title}</h2><p class="info-text">${text}</p>`)}
function init(){
 $('#searchInput')?.addEventListener('input',renderProducts);$('#searchBtn')?.addEventListener('click',()=>{renderProducts();$('#shop')?.scrollIntoView({behavior:'smooth'})});$('#sortSelect')?.addEventListener('change',e=>{sort=e.target.value;renderProducts()});$('#cartBtn')?.addEventListener('click',openCart);$('#closeCart')?.addEventListener('click',closeCart);$('#overlay')?.addEventListener('click',closeCart);$('#checkoutBtn')?.addEventListener('click',checkout);$('#accountBtn')?.addEventListener('click',account);$('#fitmentBtn')?.addEventListener('click',fitment);$('#finderBtn')?.addEventListener('click',fitment);$('#mobileMenu')?.addEventListener('click',()=>$('#categories')?.classList.toggle('open'));$('#modal')?.addEventListener('click',e=>{if(e.target.id==='modal')closeModal()});
 $('#footerAccount')?.addEventListener('click',e=>{e.preventDefault();account()});$('#ordersLink')?.addEventListener('click',e=>{e.preventDefault();showOrders()});$('#favoritesLink')?.addEventListener('click',e=>{e.preventDefault();favorites()});$('#trackOrderLink')?.addEventListener('click',e=>{e.preventDefault();track()});$('#returnsLink')?.addEventListener('click',e=>{e.preventDefault();info('الاستبدال والاسترجاع','نفحص حالة المنتج عند الاستلام. للاستبدال تواصل مع خدمة العملاء مع رقم الطلب.')});$('#contactLink')?.addEventListener('click',e=>{e.preventDefault();info('تواصل معنا','خدمة العملاء: 0770 000 0000 — واتساب والبريد يضافان عند تجهيز بيانات المتجر.')});
 document.addEventListener('click',e=>{const cat=e.target.closest('[data-cat]');if(cat){e.preventDefault();filterCategory(cat.dataset.cat)}const addBtn=e.target.closest('[data-add]');if(addBtn){e.stopPropagation();add(addBtn.dataset.add)}const fav=e.target.closest('[data-fav]');if(fav){e.stopPropagation();toggleFav(fav.dataset.fav)}const minus=e.target.closest('[data-minus]');if(minus){e.stopPropagation();qty(minus.dataset.minus,-1)}const plus=e.target.closest('[data-plus]');if(plus){e.stopPropagation();qty(plus.dataset.plus,1)}const card=e.target.closest('[data-product]');if(card&&!e.target.closest('button'))productDetails(card.dataset.product)});
 renderProducts();renderCart();
}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
